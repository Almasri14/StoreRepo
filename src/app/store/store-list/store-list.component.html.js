(function () {
    function class_1() {
    }
    return class_1;
}());
"col-xs-10 form-group" >
    Search < /label>
    < input[(ngModel)];
"query" >
;
"loginService.IsAdmin()" >
    type;
"button";
var default_1 = (function () {
    function default_1() {
    }
    return default_1;
}());
"btn btn-primary";
style = "margin-bottom: 5px"(click) = "OnAddNewCar()" > Add;
new car < /button>
    < /template>
    < template[ngIf];
"storeService.add" >
    -store - add > /app-store-add>
    < /template>
    < /div>
    < div * ngFor;
"let car of cars | filter:query";
var default_2 = (function () {
    function default_2() {
    }
    return default_2;
}());
"col-xs-4" >
    -store - item[store];
"car" > /app-store-item>
    < /div>;
